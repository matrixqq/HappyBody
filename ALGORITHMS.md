# 算法与证据 · 1.0.0

核对日期：2026-09-11。软件定位为一般成人日常活动与训练记录工具。组合后的建议引擎不是已完成临床验证的算法。

## 1. 周目标：指南规则

最近 7 个日历日（含今天）计算：

`等效有氧分钟 = 已确认中等强度分钟 + 2 × 已确认高强度分钟`

界面以 150 分钟作为一般成人参考起点；WHO 范围为中等强度 150–300 分钟或高强度 75–150 分钟或等效组合。新手循序渐进，不要求当周补齐。力量目标为至少两天，只有用户确认覆盖主要肌群且达到中等或更高强度才计入。同日多次训练不重复计天。

强度由用户根据实际活动确认；不自动把苹果 Exercise Minutes 或某个 RPE 阈值当成指南强度。未确认只标未知。

来源：[WHO 2020 Guidelines](https://www.who.int/publications/i/item/9789240015128)、[CDC 强度与谈话测试](https://www.cdc.gov/physical-activity-basics/measuring/index.html)。

## 2. Session-RPE：负荷记录

`负荷 AU = HKWorkout.duration / 60 × 整次运动主观用力评分（0–10）`

推荐运动结束后约 30 分钟回顾整次训练。0 是有效的“无用力”评分；未评分是 nil，不能当 0。显示已评分/总训练次数，避免不完整的负荷被解释为低负荷。

依据：[Foster et al., 2001, A new approach to monitoring exercise training](https://pubmed.ncbi.nlm.nih.gov/11708692/)。该方法用于量化负荷，不提供一个适用于所有人的安全负荷上限，也不能仅凭 AU 推算需要休息多少小时。

## 3. 恢复趋势：明确标注的产品启发式

研究背景：[Vesterinen 2016](https://pubmed.ncbi.nlm.nih.gov/26909534/)；[HRV 引导训练系统综述，2021](https://pmc.ncbi.nlm.nih.gov/articles/PMC8507742/)。这些研究支持进一步探索个体化调整，但不能证明本产品组合规则有效。

本版只读取 Apple 的 `heartRateVariabilitySDNN`。取所选来源 05:00–11:00、非手动录入记录的日中位数。时间筛选不能保证姿势、呼吸、设备与测量协议完全一致。未获取 RR 序列，不计算 RMSSD，也不转换 SDNN。

基线窗口：今日前第 35 至第 8 天，共 28 个日历日；至少 14 个有效日。最近信号窗口：今日及前 2 天；至少 2 个有效日，且必须有今日数据。

- HRV 在 ln(SDNN) 上计算；静息心率用原值。
- 中心 `m = median(baseline)`。
- 离散度 `s = 1.4826 × median(abs(x - m))`。
- HRV 下界 `m - 2s`；静息心率上界 `m + 2s`。
- 至少两天超界且今日超界才触发；基线 MAD 为零则不判断。

**这些窗口、倍数与组合是透明的工程选择，不是医学阈值，也不是对某项 HRV 随机试验的复现。** 未通过前瞻验证前不应作为自动增加强度的理由。

## 4. 建议优先级

1. 无当天有效自评：先完成自评。过期、未来或越界自评不参与决策。
2. 用户报告胸痛、晕厥、异常气短：暂停锻炼，提示按症状紧急程度寻求医疗帮助。
3. 自述不适/发热：休息；疲劳或酸痛 ≥4/5：优先恢复。
4. HRV 与静息心率均持续偏离，或一项偏离同时短睡眠/中等疲劳，或短睡眠且中等疲劳：降低强度或休息。
5. 原定休息且没有更高优先级问题：维持休息。
6. 单项趋势或睡眠不足：复核数据，先轻松开始，不额外加量。
7. 完整数据且未触发规则：可考虑维持既有计划，不代表医疗许可。
8. 缺失生理数据：只提供基于当日自评和一般活动指南的保守参考，标“数据不足”。

睡眠少于 7 小时参考 [CDC 成人睡眠建议](https://www.cdc.gov/sleep/about/index.html)。穿戴记录可能低估或分期错误，睡眠缺失不等于 0，也不等于充分休息。

## 5. 数据质量与隐私

- 各指标分开选来源，默认优先近 14 天记录覆盖最多、最近有数据的来源；不跨设备拼接成虚假连续基线。
- 同日心率/HRV 取中位数，重复采样不增加“有效天数”。
- 睡眠只保留 asleep 类别，合并同来源重叠区间；中午到次日中午归为一个睡眠日。夏令时按实际经过秒数计算。
- 训练只使用所选来源；重叠保守忽略后开始记录、报告数量。可能少计，不宣称与苹果汇总完全一致。
- 使用当前设备时区；跨时区旅行的数据需人工审视。
- [HealthKit 读取授权机制](https://developer.apple.com/documentation/healthkit/authorizing-access-to-health-data)：完成授权页不代表已获得读取权限，空结果可能是拒绝或没有数据。
- 权限只读；无后台/网络分析。源码不包含用户真实健康数据。
